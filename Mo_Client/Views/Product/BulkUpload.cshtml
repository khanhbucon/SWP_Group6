@{
    Layout = "~/Views/Seller/_SellerLayout.cshtml";
    ViewData["Title"] = "Tải lên sản phẩm từ file .txt";
}

<h4 class="mb-3"><i class="fas fa-file-upload me-2 text-primary"></i>Tải lên sản phẩm từ file .txt</h4>

@if(TempData["Error"]!=null){<div class="alert alert-danger">@TempData["Error"]</div>}
@if(TempData["Success"]!=null){<div class="alert alert-success">@TempData["Success"]</div>}

<form asp-action="BulkUpload" method="post" enctype="multipart/form-data" class="card shadow-sm p-3">
    <div class="row g-3">
        <div class="col-md-4">
            <label class="form-label">Chọn gian hàng</label>
            <select name="shopId" class="form-select" required>
                <option value="">-- Chọn --</option>
                @foreach (var s in (IEnumerable<Mo_Client.Services.AuthApiClient.ShopResponse>)(ViewBag.Shops ?? new List<Mo_Client.Services.AuthApiClient.ShopResponse>())){
                    <option value="@s.Id" selected="@(Equals(ViewBag.SelectedShopId, s.Id) ? "selected" : null)">@s.Name</option>
                }
            </select>
        </div>
        <div class="col-md-4">
            <label class="form-label">SubCategory Id</label>
            <input class="form-control" type="number" name="subCategoryId" required />
        </div>
        <div class="col-md-4">
            <label class="form-label">Tên mặt hàng</label>
            <input class="form-control" name="name" placeholder="VD: Clone 50 - 500bb > 8 Tháng" required />
        </div>
        <div class="col-md-3">
            <label class="form-label">Giá</label>
            <input class="form-control" type="number" step="0.01" name="price" required />
        </div>
        <div class="col-md-3">
            <label class="form-label">Tồn kho mặc định</label>
            <input class="form-control" type="number" name="stock" value="1" required />
        </div>
        <div class="col-md-3">
            <label class="form-label">Phí sàn (%)</label>
            <input class="form-control" type="number" step="0.01" name="fee" />
        </div>
        <div class="col-md-3">
            <label class="form-label">Tên biến thể</label>
            <input class="form-control" name="variantName" placeholder="Default" />
        </div>
        <div class="col-md-12">
            <label class="form-label">Chọn file .txt (mỗi dòng 1 sản phẩm)</label>
            <input class="form-control" type="file" name="file" accept=".txt" required />
            <small class="text-muted">Mỗi dòng sẽ được lưu vào mô tả cho sản phẩm/tài khoản.</small>
        </div>
    </div>
    <div class="mt-3 d-flex gap-2">
        <button class="btn btn-primary"><i class="fas fa-upload me-1"></i>Tải lên</button>
        <a asp-action="List" class="btn btn-outline-secondary">Quay lại danh sách</a>
    </div>
</form>

@if(ViewBag.Summary!=null){
    <div class="alert alert-info mt-3"><strong>Kết quả:</strong> @ViewBag.Summary</div>
}

@if(ViewBag.Results!=null){
    var list = (IEnumerable<Mo_Client.Controllers.ProductController.LineResult>)ViewBag.Results;
    <div class="card mt-3">
        <div class="card-header">Chi tiết các dòng</div>
        <div class="table-responsive">
            <table class="table table-sm mb-0">
                <thead><tr><th>#</th><th>Nội dung</th><th>Trạng thái</th><th>Lỗi</th></tr></thead>
                <tbody>
                @foreach(var r in list){
                    <tr class="@(r.Success?"table-success":"table-danger")">
                        <td>@r.Index</td>
                        <td style="white-space:pre-wrap">@r.Content</td>
                        <td>@(r.Success?"OK":"FAIL")</td>
                        <td>@r.Error</td>
                    </tr>
                }
                </tbody>
            </table>
        </div>
    </div>
}
