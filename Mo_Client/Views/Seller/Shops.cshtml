@model Mo_Client.Services.AuthApiClient.ShopResponse
@{
    ViewData["Title"] = "Gian hàng";
    Layout = "~/Views/Seller/_SellerLayout.cshtml";
}

<div class="d-flex justify-content-between align-items-center mb-3">
    <h4 class="mb-0">Gian hàng</h4>
    <a class="btn btn-primary" asp-controller="Seller" asp-action="AddShop">Thêm mới</a>
</div>

@if (Model == null)
{
    <div class="alert alert-warning">Bạn chưa tạo gian hàng nào.</div>
}
else
{
    <div class="table-responsive">
        <table class="table table-bordered align-middle">
            <thead class="table-light">
                <tr>
                    <th>Thao tác</th>
                    <th>Tên gian hàng</th>
                    <th>Loại</th>
                    <th>Trùng</th>
                    <th>Reseller</th>
                    <th>Đơn giá</th>
                    <th>Sàn</th>
                    <th>Kho</th>
                    <th>Ngày tạo</th>
                    <th>Trạng thái</th>
                </tr>
            </thead>
            <tbody>
                <tr>
                    <td class="text-nowrap">
                        <a class="btn btn-sm btn-outline-primary" asp-controller="Seller" asp-action="EditShop">Sửa</a>
                        <a class="btn btn-sm btn-outline-info" asp-controller="Seller" asp-action="ShopStats">Thống kê</a>
                    </td>
                    <td>@Model.Name</td>
                    <td>-</td>
                    <td>-</td>
                    <td>-</td>
                    <td>-</td>
                    <td>-</td>
                    <td>-</td>
                    <td>@Model.CreatedAt?.ToString("dd/MM/yyyy")</td>
                    <td>@(Model.IsActive == true ? "Hoạt động" : "Không hoạt động")</td>
                </tr>
            </tbody>
        </table>
    </div>
}
